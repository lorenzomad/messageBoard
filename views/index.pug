extends layout

block content
  h1= title 
  p Welcome to #{title} 
  each item in messages
    div 
      p= item.user
      p= item.text
      p= item.added
    br

  a(href='/new') Send a new message